/*
 * Types associated with dates and timestamps
 */

/**
 * A timestamp value, represented as the number of milliseconds since the beginning of the "Unix epoch" (Jan 1, 1970 @ midnight UTC)
 */
export type Timestamp = number;